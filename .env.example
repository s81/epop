# Turso cloud — leave blank for local SQLite dev (file:local.db)
TURSO_DB_URL=
TURSO_AUTH_TOKEN=

# Session signing — generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
